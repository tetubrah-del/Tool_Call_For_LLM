Place brand logo files in this directory with these exact names:

- `logo-ja.png` for Japanese UI (`lang=ja`)
- `logo-en.png` for International/English UI (`lang=en`)

The app will automatically switch logos by language in the global nav and home hero.
